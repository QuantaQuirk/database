{
    "name": "quantaquirk/database",
    "description": "The QuantaQuirk Database package.",
    "license": "MIT",
    "homepage": "https://quantaquirk.com",
    "support": {
        "issues": "https://github.com/quantaquirk/framework/issues",
        "source": "https://github.com/quantaquirk/framework"
    },
    "keywords": ["quantaquirk", "database", "sql", "orm"],
    "authors": [
        {
            "name": "Taylor Otwell",
            "email": "taylor@quantaquirk.com"
        }
    ],
    "require": {
        "php": "^8.1",
        "ext-pdo": "*",
        "brick/math": "^0.9.3|^0.10.2|^0.11",
        "quantaquirk/collections": "^1.0.0",
        "quantaquirk/container": "^1.0.0",
        "quantaquirk/contracts": "^1.0.0",
        "quantaquirk/macroable": "^1.0.0",
        "quantaquirk/support": "^1.0.0"
    },
    "autoload": {
        "psr-4": {
            "QuantaQuirk\\Database\\": ""
        }
    },
    "extra": {
        "branch-alias": {
            "dev-master": "10.x-dev"
        }
    },
    "suggest": {
        "ext-filter": "Required to use the Postgres database driver.",
        "doctrine/dbal": "Required to rename columns and drop SQLite columns (^3.5.1).",
        "fakerphp/faker": "Required to use the eloquent factory builder (^1.21).",
        "quantaquirk/console": "Required to use the database commands (^10.0).",
        "quantaquirk/events": "Required to use the observers with Eloquent (^10.0).",
        "quantaquirk/filesystem": "Required to use the migrations (^10.0).",
        "quantaquirk/pagination": "Required to paginate the result set (^10.0).",
        "symfony/finder": "Required to use Eloquent model factories (^6.2)."
    },
    "config": {
        "sort-packages": true
    },
    "minimum-stability": "dev"
}
